<Window x:Class="OnewheroBayVisitorManagement.Views.AnalyticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Analytics &amp; Reports" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1B5E20"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Analytics Dashboard" 
                      FontSize="28" 
                      FontWeight="Bold" 
                      Foreground="#1B5E20"
                      VerticalAlignment="Center"/>
            <Button Content="Refresh Data" 
                    Style="{StaticResource ButtonStyle}"
                    Click="RefreshData_Click"
                    Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Statistics Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Visitors Card -->
            <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="👥" FontSize="36" HorizontalAlignment="Center"/>
                    <TextBlock Text="Total Visitors" 
                              FontSize="14" 
                              Foreground="#666"
                              HorizontalAlignment="Center"
                              Margin="0,10,0,5"/>
                    <TextBlock x:Name="txtTotalVisitors" 
                              Text="0" 
                              FontSize="32" 
                              FontWeight="Bold"
                              Foreground="#2E7D32"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Total Bookings Card -->
            <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="🎫" FontSize="36" HorizontalAlignment="Center"/>
                    <TextBlock Text="Total Bookings" 
                              FontSize="14" 
                              Foreground="#666"
                              HorizontalAlignment="Center"
                              Margin="0,10,0,5"/>
                    <TextBlock x:Name="txtTotalBookings" 
                              Text="0" 
                              FontSize="32" 
                              FontWeight="Bold"
                              Foreground="#2E7D32"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Active Events Card -->
            <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="🎪" FontSize="36" HorizontalAlignment="Center"/>
                    <TextBlock Text="Active Events" 
                              FontSize="14" 
                              Foreground="#666"
                              HorizontalAlignment="Center"
                              Margin="0,10,0,5"/>
                    <TextBlock x:Name="txtActiveEvents" 
                              Text="0" 
                              FontSize="32" 
                              FontWeight="Bold"
                              Foreground="#2E7D32"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Total Revenue Card -->
            <Border Grid.Column="3" Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="💰" FontSize="36" HorizontalAlignment="Center"/>
                    <TextBlock Text="Total Revenue" 
                              FontSize="14" 
                              Foreground="#666"
                              HorizontalAlignment="Center"
                              Margin="0,10,0,5"/>
                    <TextBlock x:Name="txtTotalRevenue" 
                              Text="$0.00" 
                              FontSize="32" 
                              FontWeight="Bold"
                              Foreground="#2E7D32"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Detailed Reports -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Popular Events -->
            <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20"
                    Margin="0,0,10,0"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="📈 Popular Events" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="#1B5E20"
                              Margin="0,0,0,15"/>

                    <DataGrid x:Name="dgPopularEvents"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="Horizontal"
                             AlternatingRowBackground="#F5F5F5"
                             HeadersVisibility="Column"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Event Name" Binding="{Binding EventName}" Width="*"/>
                            <DataGridTextColumn Header="Bookings" Binding="{Binding BookingCount}" Width="80"/>
                            <DataGridTextColumn Header="Revenue" 
                                              Binding="{Binding Revenue, StringFormat='${0:F2}'}" 
                                              Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Visitor Demographics -->
            <Border Grid.Column="1" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20"
                    Margin="10,0,0,0"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="🌍 Visitor Demographics" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="#1B5E20"
                              Margin="0,0,0,15"/>

                    <TextBlock Text="Top Cities" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              Margin="0,0,0,10"/>

                    <DataGrid x:Name="dgCities"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="Horizontal"
                             AlternatingRowBackground="#F5F5F5"
                             HeadersVisibility="Column"
                             Height="200"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="City" Binding="{Binding City}" Width="*"/>
                            <DataGridTextColumn Header="Visitors" Binding="{Binding Count}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Text="Popular Interests" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              Margin="0,20,0,10"/>

                    <DataGrid x:Name="dgInterests"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="Horizontal"
                             AlternatingRowBackground="#F5F5F5"
                             HeadersVisibility="Column"
                             Height="200"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Interest" Binding="{Binding Interest}" Width="*"/>
                            <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>